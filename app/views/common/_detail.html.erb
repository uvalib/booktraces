<% if logged_in? %>
   <div class="pure-g">
      <div class="pure-u-2-3">
         <%= render "/common/general" %>
      </div>
      <div class="pure-u-1-3">
         <%= render "/common/status" %>
         <%= render "/common/cataloging" %>
      </div>
   </div>
<% else %>
   <%= render "/common/general" %>
<% end %>
